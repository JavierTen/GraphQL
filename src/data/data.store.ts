import authors from "./papers.json";
import papers from "./papers.json";
import references from "./references.json";

export const database = {
    authors,
    papers,
    references
}


export default database;